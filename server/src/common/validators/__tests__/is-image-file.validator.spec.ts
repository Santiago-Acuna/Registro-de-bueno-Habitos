import { IsImageFileConstraint } from '../is-image-file.validator';

describe('IsImageFileConstraint', () => {
  it('should be defined', () => {
    expect(IsImageFileConstraint).toBeDefined();
  });
});